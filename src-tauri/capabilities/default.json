{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "deep-link:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/konigslibrary-server",
          "sidecar": true
        }
      ]
    }
  ]
}

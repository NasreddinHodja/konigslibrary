{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "rustup target add wasm32-unknown-unknown && cargo install -f wasm-bindgen-cli --version 0.2.99 && bun install --frozen-lockfile",
  "buildCommand": "bun run build"
}
